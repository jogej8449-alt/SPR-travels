const Drivers = () => {
    return (
        <div style={{ padding: '2rem 5%' }}>
            <h2 className="heading text-gradient">Manage Drivers</h2>
            <div className="glass-panel" style={{ padding: '2rem' }}>
                <button className="btn-primary" style={{ marginBottom: '1.5rem' }}>+ Add New Driver</button>
                <p style={{ color: 'var(--text-secondary-dark)' }}>Driver list will populate here...</p>
            </div>
        </div>
    );
};

export default Drivers;
