const Users = () => {
    return (
        <div style={{ padding: '2rem 5%' }}>
            <h2 className="heading text-gradient">Manage Users</h2>
            <div className="glass-panel" style={{ padding: '2rem' }}>
                <p style={{ color: 'var(--text-secondary-dark)' }}>User list will populate here...</p>
            </div>
        </div>
    );
};

export default Users;
